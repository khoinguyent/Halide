/// Gear gallery photos are for on-screen display only — keep uploads small to save user storage (R2).
/// [image_picker] resizes/compresses before read; values align with backend JPEG normalization.
const int kGearImagePickerMaxDimension = 1280;
const int kGearImagePickerQuality = 75;
